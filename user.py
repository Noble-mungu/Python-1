class User:

    """
    class that generates new instance of the user
    """
    user_list = []

    def __init__(self,user_name,password):
        self.user_name = first_name,last_name
        self.password = password

    def save_user (self):

        """
        save user method saves a  new user to the user list 
        """
        User.user_list.append(self)

class Credentials:
    """
    Class to creatre account credentials,generte password and save.

    """

    credentials_list = []
    
    def check_user(user_name,password):
        """
        Method that checks if the name and password entered match in user_list

        """
        return
        User.user_list()
        
